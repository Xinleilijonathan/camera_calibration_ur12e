"""Camera calibration package for the UR12e + AprilTag grid workflow.

Importing anything from this package never opens a camera and never connects
to a robot. Hardware is only touched by explicit open()/connect() calls.
"""
